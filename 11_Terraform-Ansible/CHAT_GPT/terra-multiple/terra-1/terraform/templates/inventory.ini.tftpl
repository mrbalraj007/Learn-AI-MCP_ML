# Generated by Terraform — DO NOT EDIT DIRECTLY

[linux]
%{ for key, vm in vms ~}
%{ if vm.os_type == "ubuntu" ~}
${vm.name} ansible_host=${vm.public_ip} ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/devtools-key.pem
%{ endif ~}
%{ endfor ~}

[windows]
%{ for key, vm in vms ~}
%{ if vm.os_type == "windows" ~}
${vm.name} ansible_host=${vm.public_ip} ansible_user=Administrator ansible_password=TempP@ssw0rd123! ansible_connection=winrm ansible_winrm_server_cert_validation=ignore ansible_port=5986 ansible_winrm_scheme=https
%{ endif ~}
%{ endfor ~}

[devtools:children]
linux
windows