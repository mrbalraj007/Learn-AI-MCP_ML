[defaults]
inventory = inventory/aws_ec2.yml
host_key_checking = False
remote_user = ubuntu
private_key_file = ~/.ssh/devtools-key.pem
interpreter_python = auto_silent

[inventory]
  enable_plugins = ini, yaml, amazon.aws.aws_ec2
